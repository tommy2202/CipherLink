name: universaldrop_app
description: UniversalDrop Flutter client (minimal scaffold).
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cryptography: ^2.7.0
  file_picker: ^8.1.2
  shared_preferences: ^2.3.2
  path_provider: ^2.1.4
  file_selector: ^1.0.3
  photo_manager: ^3.1.1
  share_plus: ^10.0.2
  mime: ^1.0.5
  path: ^1.9.0
  archive: ^3.6.1
  flutter_webrtc: ^0.10.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
